function showMenu() {
    let navLinks = document.getElementById('navLinks');
    navLinks.style.right = "0";
}

function hideMenu() {
    let navLinks = document.getElementById('navLinks');
    navLinks.style.right = "-200px";
}